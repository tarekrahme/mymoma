<div class="container">
  <h1>Edit transaction</h1>
  <%= simple_form_for(@goal) do |f| %>
    <div class="form-inputs">
      <%= f.input :name %>
      <%= f.input :amount_cents, label: "Amount (£)" %>
      <%= f.input :completion_date, as: :date %>

   <div class="form-actions">
        <%= f.button :submit, "Update goal", class:"btn btn-primary" %>
    </div>
  <% end %>
  </div>

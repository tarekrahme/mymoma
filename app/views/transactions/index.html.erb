<div class="container">
  <h1 class="my-4 text-center">Transactions:</h1>
  <div class="transactions-box w-100">
    <% @transactions.each do |transaction| %>
      <%= link_to transaction_path(transaction) do %>
        <div class="d-flex display-inline justify-content-between my-3">
          <h4 class = "ml-3"><%= transaction.merchant %></h4>
          <h4 class="">£<%= transaction.amount_cents.to_f/100 %></h4>
        </div>
      <% end %>
    <% end %>
  </div>
  <%= link_to "Add a transaction", new_transaction_path %>
  <br>
  <%= link_to "Dashboard", wallet_path(current_user.wallet) %>
</div>

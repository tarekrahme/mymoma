<div class="container">
  <h1>Edit transaction</h1>
  <%= simple_form_for(@transaction) do |f| %>
    <div class="form-inputs">
      <%= f.input :merchant %>
      <%= f.input :amount_cents, label: "Amount (£)" %>
      <%= f.simple_fields_for :day, @transaction.day do |t| %>
        <%= t.input :date, as: :string, input_html: { class: 'datepicker' } %>
      <% end %>
    </div>

   <div class="form-actions">
        <%= f.button :submit, "Update transaction", class:"btn btn-primary" %>
    </div>
  <% end %>
  </div>

<div class="container">
  <div class="transaction-show text-center mt-5"; style='position: relative'>
    <h1>£<%= @transaction.amount_cents / 100.0 %></h1>
    <br>
    <h5>at</h5>
    <h2> <%= @transaction.merchant.capitalize %></h2>
    <br>
    <h5>on</h5>
    <h2> <%= @transaction.day.date.strftime('%a %d %b') %></h2>
  </div>

  <div>
    <%= link_to 'Edit', edit_transaction_path(@transaction),class:"btn btn-primary btn-main flex"  %>
    <%= link_to transaction_path(@transaction), method: :delete, data: { confirm: 'Are you sure you want to delete this transaction?' }, style:"position: absolute; top: 110px; right: 50px" do %>
      <div style="font-size: 32px"><i class="fas fa-trash-alt"></i></div>
    <% end %>
  </div>
  <%= link_to 'All transactions', transactions_path, class:"btn btn-primary btn-main" %>
</div>
